# 센서 로거 검증 리포트 (2026-09-03 16:38)
- 데모: 가상 2채널 × 600초, **결함을 알고 심음**(결측 3구간 · 범위밖 5 · 정체 1) → 감시기가 그것만 정확히 잡는지

| 검증 | 결과 |
|---|---|
| ① 저장 유실 0 | 발생 1120 = 저장 1120 → PASS |
| ② 재적재 중복 0 (PK) | 재적재 신규 0건 → PASS |
| ③ 결측 구간 검출(심은 3) | temp 2 + dist 1 = 3, 위치 일치 → PASS |
| ④ 범위 이상값 검출(심은 5) | temp 3 + dist 2 → PASS (저장은 유지·플래그만) |
| ⑤ 정체 검출(심은 1: 40회 연속) | dist 1건(40회) · temp 오탐 0 → PASS |
| ⑥ 재현성(2회 동일) | OK |

## 감시 리포트 출력 예
```
[dist] 수신 570 · 결측 1구간 · 이상값 2 · 정체 1
  · 결측 31.0초: ts 1700000249 → 1700000280
  · 이상값 ts 1700000090 = -50.0 (범위 밖, 저장·플래그)
  · 이상값 ts 1700000510 = -50.0 (범위 밖, 저장·플래그)
  · 정체 ts 1700000350부터 40회 연속 777.0 (센서 고착 의심)
[temp] 수신 550 · 결측 2구간 · 이상값 3 · 정체 0
  · 결측 31.0초: ts 1700000119 → 1700000150
  · 결측 21.0초: ts 1700000399 → 1700000420
  · 이상값 ts 1700000060 = 500.0 (범위 밖, 저장·플래그)
  · 이상값 ts 1700000200 = 500.0 (범위 밖, 저장·플래그)
  · 이상값 ts 1700000333 = 500.0 (범위 밖, 저장·플래그)
```

- ※ 데모 = 가상 센서(어댑터 분리). 실물 연결 = 장비의 파일/시리얼 출력에 어댑터 1회 맞춤(하드웨어 필요).