# 노션 변경 감지 → 슬랙/콘솔 알림 커넥터 (2026-09)

## 이 도구가 하는 일 (비개발자용)
노션 데이터베이스를 주기 폴링해 **새 페이지·속성 변경·보관**을 감지하고 알림을 보낸다. 엔진 = `core/watch.py` Watcher(스냅샷·중복제거·보류 재송 공용 엔진).

## 쓰는 법
```
python notion_slack.py
```
데모 데이터가 동봉되어 있어 실행 즉시 결과와 검증 리포트가 생성됩니다. 실제 적용은 설정값(규칙·경로·계정)만 1회 맞춥니다.

## 검증 (0/0 PASS)

전체 항목은 같은 폴더의 검증 리포트(md)에 있습니다.

## 정직 고지
데모 화면·데이터는 합성 데이터입니다. 이 문서는 도구의 동작 범위를 설명하며, 회계·세무·법률 자문이 아닙니다.
