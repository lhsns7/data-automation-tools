# 데이터 이관 검증 리포트 (2026-09-03 18:39)
- 데모 = 원본 1,000건(구시스템) → 이관 시뮬 2벌: **클린본**(컬럼명·금액 콤마·날짜 형식·공백 전부 변경 = 정상 변화만) / **결함본**(누락 3·중복 2·여분 1·필드 훼손 4 심음)

| 검증 | 결과 |
|---|---|
| ① 클린본(표기만 변화) → 차이 0 = ★오탐 0 | PASS |
| ② 심은 누락 3건 → 키 정확 검출 | PASS |
| ③ 심은 여분 1·중복 2 → 정확 검출 | PASS |
| ④ 심은 필드 훼손 4건(금액+900·날짜 월시프트·잘림·문자훼손) → 키·필드 정확 검출 | PASS |
| ⑤ 합계 항등 안전망(금액 총합 67,442,800 vs 67,175,901) | 차이 검출 → PASS |
| ⑥ 재현성(2회 동일) | OK |
| 산출 | 이관검증_데모.xlsx (5시트) |

- ※ 표기 차이(콤마·날짜 형식·공백·컬럼명)는 정규화로 흡수 — 정규화 없는 diff는 오탐 수천 건으로 무의미.
- ※ 키·필드 매핑·정규화 규칙 = 시스템 쌍마다 1회 설정. 실이관 검증도 같은 리포트 형식으로 납품.